Pre-existing veripb failures found by the paired sweep (cmp2.bash), 2026-08-05.
All 27 occur identically on 39ca857 and on a24f90a: every proof in the sweep was
byte-identical between the two binaries, so these are unrelated to M4.2.

Instances are generated by build/create_random_graph (from the same tree):
  dense/N      pattern: create_random_graph --seed N 5 0.6
               target : create_random_graph --seed (N+100) 10 0.9
  equalind/N   pattern: create_random_graph --seed N 8 0.5
               target : create_random_graph --seed (N+100) 8 0.5
  bigdense/N   pattern: create_random_graph --seed N 7 0.6
               target : create_random_graph --seed (N+100) 14 0.85
  loopydense/N pattern: create_random_graph --seed N --loops 0.4 5 0.6
               target : create_random_graph --seed (N+100) --loops 0.4 10 0.85
  cliques/N    pattern: create_random_graph --seed N 5 0.7
               target : 4 disjoint K4s (vertices 4c..4c+3, c=0..3)
The exact files used are in instances/ (p_<fam>_<seed>.csv, t_<fam>_<seed>.csv).

Common prefix for every run: --format csv --no-clique-detection <opts> --prove <out>

CLASS A -- not a failure, sweep-script artefact (14 cases)
  opts: --n-exact-path-graphs 3 --shape shape_tri.csv
  instances: dense/1,2,3  equalind/2,4  cliques/5  bigdense/1..5  loopydense/1,2,3
  veripb says: "s UNDER ASSERTIONS SATISFIABLE" + "Warning: The proof used
  unchecked assertions".  The sweep greps for "VERIFIED", which this output does
  not contain.  Real caveat: --shape proofs contain assertions veripb does not check.

CLASS B -- veripb timeout, not a failure (5 cases)
  opts: --n-exact-path-graphs 6 --count-solutions
  instances: bigdense/1..5
  veripb killed by the sweep's 120 s timeout (still running at 180 s).  Enumeration
  proofs on a 14-vertex 0.85-density target.

CLASS C -- genuine proof failure: RUP (3 cases)
  opts: --n-exact-path-graphs 6 --induced                     -> equalind/2, equalind/4
        --n-exact-path-graphs 6 --induced --staged
          --staged-first-round-backtracks 2                   -> equalind/4
  status = false, nodes = 0 (refuted at the compatibility stage).  The final
  "@unsatconc rup >= 1 ;" is not RUP-derivable from the database.
  Rerun with veripb --trace-failed to see the propagations.

CLASS D -- genuine proof failure: no proof body (5 cases)
  opts: --noninjective
  instances: loopydense/1..5
  used_loops_property = true: the target-loop shortcut concludes SAT before any
  proof is emitted, so the .pbp is just the header plus "f <n> ;" and veripb
  reports a syntax error (EOF at line 3).
